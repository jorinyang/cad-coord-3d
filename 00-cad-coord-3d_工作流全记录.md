# CAD 坐标拆解与 3D 还原 · 工作流全记录

| 项目 | 内容 |
| --- | --- |
| 工作流 | answer（AI Native'S Workflower） |
| 领域识别 | 🏢 新业务/产品（构建一个网页应用） |
| 启动日期 | 2026-09-11 |
| 当前阶段 | Phase 6 Build（14/16；bug 修复 + 演示图纸 + 取景修复均已上线；DWG→DXF 六层验证 ✓；榫卯格构 647 节点图纸 ✓；项目已上传 GitHub（v0.9.0）；C7b 等用户确认 Docker；P4 Review 预审完） |
| 版本 | v0.1 |

---

## 0. 需求初述

> 我想做一个网页应用，我想实现三个能力，分别是：
> 1. 拆解 CAD 图纸，将其中的拆解为三维坐标与坐标点关系。
> 2. 将三维坐标存储为：坐标名称 / X 坐标值 / Y 坐标值 / Z 坐标值 / 产生链接的坐标名称。
> 3. 三维坐标 + 坐标点关系 3D 演示还原。

## 0.1 盘点结论（能查到的不问）

- 本机桌面全域无 `.dxf` / `.dwg` 文件；历史会话无相关项目痕迹 → 全新需求，样例素材需向用户索取
- 数据模型字段（坐标名称 / X / Y / Z / 关联坐标名称）以用户原述为准

## 1. 决策树全景

| 组 | 要定的 | 状态 |
| --- | --- | --- |
| ① 输入 | 图纸形态 | ✅ 已定 |
| ② 拆解 | 拆解对象与提取规则、使用流程 | ✅ 已定 |
| ③ 存储 | 存储载体与数据模型 | ✅ 已定（云端） |
| ④ 演示 | 3D 保真度与交互 | ✅ 已定（骨架级） |
| ⑤ 交付 | 能力边界、反目标、后端形态与部署 | ✅ 主体已定（部署方案待 Architect 提案） |

## 2. 决策记录

### Q1 · 图纸形态 ✅

- 用户答复（原文）：「以上几种都有可能先解决第一种」
- 结论：本次先解决**三维 CAD 文件（.dwg / .dxf，图形本身带三维坐标）**；二维图纸、PDF/图片等形态列为未来可能扩展，不纳入本次范围（以 Brief 为准）。

### Q2 · 拆解对象与提取规则 ✅

- 用户答复：**两者混合：几何全提，图上有的名称优先用、没有的自动补编号**
- 结论：
  - 坐标点：图纸几何顶点全提（线段端点、多段线顶点等，实体范围届时技术细化）
  - 关系：几何连线（两点间有连线即建立关系）
  - 名称：优先取图纸已有标注（文字/编号，就近关联），缺失时自动补编号
- 影响：解析器需具备「几何提取 + 名称关联（文字就近匹配）」双通道能力

### Q3 · 使用场景与定位 ✅

- 用户答复：**能力储备——先把「图纸→坐标→3D」链路做出来，将来哪里需要哪里用**
- 结论：通用工具/能力建设定位，非单一客户交付；设计取向 = 通用性优先、链路先跑通、可复用可展示
- 影响：不针对单一业务场景特化；架构取「轻起点、可扩展」路线

### Q4 · 拆解使用流程与技术选型 ✅

- 用户答复（初次）：**工程最优就好——你来定路径，我只要能稳定跑通**
- 用户修正（Q5 轮补充）：**还是要增加 DWG 的后端** → 网页端直接支持 DWG 上传，DWG 交由后端处理
- 结论：
  - 技术选型授权给 Agent，唯一硬标准 = 稳定跑通
  - 架构方向（待 Architect 固化）：前端 SPA + 后端解析服务；DXF 前端可直解，DWG 走后端转换解析
- 影响：需规划后端部署（云函数 / 本地服务）；解析链路覆盖 DWG → DXF → 坐标数据

### Q5 · 3D 演示效果层级 ✅

- 用户答复：**本轮仅需要实现骨架级**
- 结论：3D 还原目标 = 骨架级（坐标点 + 连接线，旋转缩放、点选查看名称/坐标）；体量级/逼真级为后续升级项
- 影响：前端 3D 采用 Three.js 点线渲染；无需截面/体量数据

### Q6 · 存储载体 ✅

- 用户答复：**云端留存：数据存到云端（跨设备访问、可分享、便于后续系统对接）**
- 结论：坐标数据存云端数据库；跨设备访问、可分享、可对接
- 影响：→ 需云数据库 + 数据 API；→ 与 DWG 后端合并规划（解析 + 存取一体）；→ 交付组倾向公网部署

### Q7 · 能力范围 ✅

- 用户答复：**以上能力全要**（多图纸管理 / 数据修正 / 导出文件 / 分享查看）
- 结论：本轮范围 = 主链路（导入→拆解→云端存储→3D 演示）+ 图纸库管理 + 数据修正 + 导出（Excel/CSV/JSON）+ 分享查看
- 影响：→ 数据模型需支持多图纸（图纸表 + 坐标表 一对多）；→ 需列表/管理界面、编辑界面、导出功能、只读分享链路

### Q8 · 边界与反目标 ✅

- 明确不做：**不编辑图纸**（只读取拆解，不修改 CAD 原图数据）
- 未勾选三项（行业语义层 / 多人协作 / 移动端适配）→ 按「未排除、本轮不承诺」处理
- 结论：工具为只读解析器定位；「数据修正」仅作用于拆解结果数据，不触碰原图

---

## 2.5 Clarify 收口 · 假设与资料

**待验证（技术，Brief/Architect 阶段处理）**
- DWG → DXF → 坐标 的后端解析链可行性（工具链选型待定）
- 名称就近匹配准确率（依赖图纸文字布局与规范程度）
- 大图纸性能（点数规模未知，暂无样例）

**默认假设（用户审查 Brief 时可修改）**
- 单人使用 + 分享链接只读查看
- 桌面浏览器优先
- 数据修正形态 = 表格编辑为主，3D 视图点选为辅
- 未排除三项（行业语义层 / 多人协作 / 移动端）不列入本轮承诺

**资料请求**
- 任何时候有真实 `.dwg` / `.dxf` 样例，直接提供（对解析器验证价值最大）

## 3. 阶段进度

- [x] Phase 1 Clarify —— 完成（8 决策点，收口已确认）
- [x] Phase 2 Brief —— 完成（结构化简报已批准）
- [x] Phase 3 Architect —— 完成（结构设计已确认）
- [x] Phase 4 Standards —— 完成（标准规范已确认）
- [x] Phase 5 Decompose —— 完成（任务清单已批准）
- [ ] Phase 6 Build —— 进行中（16 任务，已开工）
- [ ] Phase 7 Review
