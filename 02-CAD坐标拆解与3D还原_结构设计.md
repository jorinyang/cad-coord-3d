# CAD 坐标拆解与 3D 还原 · 结构设计

| 项 | 内容 |
| --- | --- |
| 代号 | cad-coord-3d |
| 日期 | 2026-09-11 |
| 上游 | 01-CAD坐标拆解与3D还原_结构化简报（已批准） |
| 状态 | 已确认（2026-09-11） |

---

## 一、结构总览

```
CAD 坐标拆解与 3D 还原系统
│
├── ① 前端 SPA（静态托管 · 浏览器）
│   ├── 图纸库（列表 + 上传入口）
│   ├── 上传解析流（拖拽 → 解析进度 → 结果）
│   ├── 图纸详情
│   │   ├── 3D 查看器（点+线、旋转缩放、点选看名称坐标）
│   │   ├── 数据表（名称 / X / Y / Z / 关联名称；编辑修正）
│   │   └── 操作区（导出 / 分享 / 重命名 / 删除）
│   └── 分享快照页（只读路由，凭 token 打开）
│
├── ② 解析服务（Python · 本地 or 云函数同构）
│   ├── API 层（FastAPI）
│   │   ├── POST   /api/parse                    上传 DWG/DXF → 转换+解析+入库
│   │   ├── POST   /api/drawings/{id}/update     编辑修正（改名 / 删点 / 删边）
│   │   ├── PATCH  /api/drawings/{id}            重命名
│   │   ├── DELETE /api/drawings/{id}            删除（级联清理点/边/快照）
│   │   ├── POST   /api/drawings/{id}/share      生成分享快照
│   │   ├── DELETE /api/share/{token}            撤销分享
│   │   └── GET    /api/health                   健康检查
│   ├── 转换层（DWG→DXF：LibreDWG 主 / ODA 备 / 手动另存兜底）
│   ├── 解析层（ezdxf：实体提取 → 顶点归一 → 连线构建 → 名称匹配）
│   └── 写库层（service key 写 Supabase）
│
├── ③ 数据层（Supabase Postgres）
│   ├── cad_drawings（图纸元数据）
│   ├── cad_points（坐标点：名称 / X / Y / Z）
│   ├── cad_links（连接关系：from → to）
│   └── cad_share_snapshots（分享快照：token + payload）
│
└── ④ 部署（阿里云）
    ├── OSS 静态托管（前端）
    └── FC 函数计算 · 容器镜像（解析服务）
```

## 二、模块关系与依赖

| 模块 | 依赖 | 被依赖 | 备注 |
| --- | --- | --- | --- |
| 前端 · 图纸库 | 数据层（只读） | — | anon key 直连读列表 |
| 前端 · 上传解析流 | 解析服务 | 图纸库 | 失败时给出兜底指引 |
| 前端 · 详情 / 3D | 数据层（读）+ 解析服务（写） | — | 读直连、写经后端 |
| 前端 · 分享快照页 | 数据层（只读） | — | token 查快照渲染 |
| 解析服务 | 转换层、解析层、写库层 | 前端 | 全系统唯一写权限持有者 |
| 转换层 | LibreDWG / ODA / 手动兜底 | — | 抽象接口 + 自动回退 |
| 数据层 | — | 前端、解析服务 | 读 = anon 只读 RLS；写 = service key |

**读写分治（核心边界）**：前端全部只读（anon key + 只读 RLS）；所有写操作（入库 / 编辑 / 删除 / 分享）经解析服务（service key）。安全边界清晰，也为将来「访问控制升级」留好收口点。

## 三、关键路径（用户流）

### 路径 1 · 上传拆解
1. 打开网页 → 图纸库
2. 拖入 .dwg / .dxf → 显示解析进度
3. 解析服务：DWG→DXF（如需）→ ezdxf 解析 → 名称匹配 → 写入数据层
4. 完成 → 自动跳转该图详情（3D + 数据表）
5. 失败 → 明确错误提示 + 建议（本机另存 DXF 后重试）

### 路径 2 · 查看 3D
1. 图纸库点开一张 → 详情
2. 3D 视图：旋转 / 缩放；点选坐标点 → 显示名称 + X / Y / Z
3. 数据表按需展开（名称 / X / Y / Z / 关联名称）

### 路径 3 · 修正编辑
1. 详情 → 编辑（表格改名 / 删点 / 删边）
2. 保存 → 解析服务写回 → 3D 与表格同步刷新

### 路径 4 · 导出
1. 详情 → 导出 CSV / XLSX / JSON → 浏览器下载

### 路径 5 · 分享
1. 详情 → 生成分享 → 得到链接（快照 token）
2. 对方打开（无需登录）→ 只读 3D + 数据表
3. 撤销 → 链接立即失效

### 路径 6 · 管理
1. 图纸库 / 详情 → 重命名 / 删除（级联清理点、边、快照）

## 四、信息层级

### 图纸库
1. 图纸列表（名称 / 点数 / 时间）— 主内容
2. 上传入口 — 最高频动作，常驻显眼
3. 空态引导 — 首次使用引导上传第一张

### 图纸详情
1. 3D 视图 — 主角，占最大区域
2. 操作区（导出 / 分享 / 重命名 / 删除）— 次级
3. 数据表 — 默认折叠，需要时展开

### 分享页
1. 3D 视图 — 第一眼
2. 数据表（只读）— 备用
3. 来源与生成时间 — 尾注

## 五、技术选型决策

| 项 | 选型 | 理由 |
| --- | --- | --- |
| 前端 | 原生 ES Modules + Three.js（本地 vendor，无构建链） | 保持"随手可开"传统；OSS 目录直传即发布 |
| 3D | Three.js（Points + LineSegments + OrbitControls + Raycaster 点选） | 骨架级最合适工具；白底极简视觉 |
| 后端 | Python 3.11 + FastAPI + ezdxf | ezdxf 是 DXF 解析事实标准；本机已有 FastAPI / uvicorn |
| DWG 转换 | LibreDWG（主）/ ODA File Converter（备）/ 手动另存（兜底） | 轻量优先 + 兼容性回退 + 终极兜底，三级保"稳定跑通" |
| 数据 | Supabase Postgres（复用现有项目 + cad_ 前缀表） | 已有项目与全套管理凭据（含 PAT）；免新开 |
| 读写 | 读 = 前端直连（anon 只读 RLS）；写 = 后端 service key | 安全边界清晰 |
| 分享 | 快照表（token + payload）+ 只读路由 | 语义清晰、可撤回、不依赖 OSS 写权限 |
| 部署 | OSS 静态 + FC 容器镜像；本地 FastAPI 为开发 / 兜底形态 | 按量低成本；低频使用最优 |

## 六、数据模型（Supabase · cad_ 前缀）

| 表 | 字段 | 说明 |
| --- | --- | --- |
| cad_drawings | id / name / source_filename / source_format / status / point_count / link_count / created_at / updated_at | 图纸元数据 |
| cad_points | id / drawing_id / name / x / y / z / seq | 坐标点 |
| cad_links | id / drawing_id / from_point_id / to_point_id | 连接关系 |
| cad_share_snapshots | token / drawing_id / payload jsonb / created_at | 分享快照 |

RLS：四表对 anon 仅开放 SELECT；写入由后端 service key 完成。

## 七、被拒替代方案（架构级）

| # | 方案 | 拒绝理由 |
| --- | --- | --- |
| 1 | FC 只做 DWG 转换、DXF 由前端解析（dxf-parser JS） | 解析逻辑分叉成两份（JS+Python）或质量下降；统一 ezdxf 一份代码 |
| 2 | 数据存 OSS JSON 文件（无数据库） | 多图纸管理 / 编辑 / 并发能力弱；无查询能力 |
| 3 | 分享 = 动态活链（直接读主数据） | 无法表达"分享版本"、撤回语义弱；快照语义更清晰 |
| 4 | 轻型服务器（ECS）常驻后端 | 月费 vs 低频使用不经济；FC 按量优先，不达标再升级 |
| 5 | 单一转换引擎（LibreDWG-only 或 ODA-only） | 前者版本兼容无回退；后者容器体积 / 许可附加更重；故取三级链 |

## 八、风险与回退

| # | 风险 | 等级 | 回退 / 预案 |
| --- | --- | --- | --- |
| 1 | 云端容器内 DWG 转换不可靠 | 高 | 三级转换链；Build 首任务验证；不行则先上线本地转换模式 |
| 2 | 新版 DWG（2018+）兼容性 | 中 | ODA 覆盖；样例实测；失败时提示手动转 DXF |
| 3 | 名称就近匹配准确率 | 中 | 阈值可调；编辑修正兜底（已在范围内） |
| 4 | 大图纸（云函数超时 / 内存） | 中 | 大小限制 + 提示；超限引导本机转 DXF；本地模式可兜 |
| 5 | 云函数冷启动慢 | 低 | 进度文案缓解；必要时保活 |
| 6 | 数据暴露（anon 只读） | 低 | 只读 + obscurity；升级路径 = 后端鉴权墙 |

## 九、环境需求清单（Build 前）

| # | 项 | 状态 |
| --- | --- | --- |
| 1 | 样例图纸 ×N（.dwg / .dxf） | 待用户提供（对解析器标定价值最大） |
| 2 | ODA File Converter 本机安装（Windows · 免费） | 待安装（Build 首任务验证） |
| 3 | Supabase：复用现有项目（cad_ 前缀表）；PAT 已有 | 就绪 |
| 4 | 阿里云：OSS 桶（已有）；FC 部署凭据 | OSS 就绪；FC 凭据待确认 |
| 5 | Python 依赖（ezdxf / fastapi / uvicorn / supabase） | pip 可达，可自动安装 |

## 十、增长适配

- 图纸库：先简单列表；数量增长后加搜索 / 分页 / 标签
- 解析格式：v1 = DWG / DXF；扩展位 = 2D 图 / PDF（另立项）
- 3D 升级：骨架 → 体量级（补截面信息）路径预留
- 访问控制：轻口令 → 后端鉴权墙 升级路径预留
- 多端：桌面为主；基础响应式适配
