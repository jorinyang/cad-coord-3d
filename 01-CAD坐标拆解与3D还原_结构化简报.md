# CAD 坐标拆解与 3D 还原 · 结构化简报

| 项 | 内容 |
| --- | --- |
| 代号 | cad-coord-3d |
| 日期 | 2026-09-11 |
| 来源 | Phase 1 Clarify 8 个决策点（全过程见 00-cad-coord-3d_工作流全记录） |
| 状态 | 已批准（2026-09-11） |

---

## 一、问题定义

CAD 图纸里的结构信息（点位、坐标、连接关系）目前锁在图纸文件中——只能开着专业软件人眼看，无法轻量提取成数据、跨设备查看、分享给别人、或在 3D 里直观还原。本项目建一条通用链路：**上传图纸 → 自动拆解为「坐标点 + 连接关系」数据 → 云端留存 → 3D 立体还原**，把图纸结构变成"看得见、取得出、传得开"的数据资产。

## 二、目标方案（体验描述）

打开网页 → 上传一份 CAD 图纸（.dwg / .dxf）→ 稍候，图纸被拆解成坐标清单（名称 / X / Y / Z / 关联名称；图上原有名称优先保留，缺失自动编号）→ 自动存进云端图纸库 → 任意时刻打开任意一张：3D 视图里旋转缩放查看结构、点选坐标点看名称与坐标 → 需要时导出数据（Excel / CSV / JSON）、或生成只读链接分享给别人 → 发现提取误差，直接在网页上修正（改名、删点、调关系）。全程不触碰 CAD 原图。

## 三、核心原则（3 条）

1. **只读解析、不碰原图** — 一切修正只作用于拆解结果，CAD 原图永远不动；化解"想改"与"怕动原数据"的张力。
2. **跑通优先、留好升级位** — 本轮骨架级 3D + 核心链路先跑通；体量级渲染、纯前端直解 DWG、行业语义等作为后续升级项，接口预留；化解"效果想象"与"落地成本"的张力。
3. **通用提取、不猜语义** — 只做"几何 + 名称"的通用提取，图上有什么读什么，不臆测业务含义；化解"图纸千差万别"与"自动化准确率"的张力。

## 四、被拒绝的替代方案（探索记录）

| # | 替代方案 | 拒绝理由 | 探索于 |
| --- | --- | --- | --- |
| 1 | 纯前端路线（浏览器内直接解析 DWG） | DWG 为二进制私有格式，浏览器端无可靠解析；用户明确要求增加 DWG 后端 | Q4 / Q5 |
| 2 | 本地手工转换（DWG 先转 DXF 再上传） | 多一步手工操作，违背"上传即拆解"；用户修正为 DWG 走后端 | Q4 / Q5 |
| 3 | 3D 直接做体量级 / 逼真级 | "点 + 关系"数据不含截面信息，无法直接出体量；用户定本轮骨架级 | Q5 |
| 4 | 纯导出路线（不存云端） | 用户选择云端留存（跨设备 / 分享 / 对接）；导出保留为子能力 | Q6 |

## 五、JTBD 价值主张

| 场景 | 要完成的任务 | 现状痛点 | 本工具价值 |
| --- | --- | --- | --- |
| 拿到一份图纸想吃透结构 | 提取点、坐标、连接关系的结构化数据 | 开 CAD 人工看点抄数，慢且易错 | 上传即拆解，输出标准坐标数据 |
| 把结构讲给别人 | 3D 直观展示 | 平面图难懂，专业软件门槛高 | 网页 3D 骨架还原，链接直达 |
| 图纸越攒越多 | 管理、回看、复用 | 文件散落无沉淀 | 云端图纸库，跨设备访问 |
| 拆解有几处不准 | 快速修正 | 回 CAD 改图成本高 | 网页改数据（不动原图） |
| 数据给别的系统用 | 导出标准格式 | 手工整理 | Excel / CSV / JSON 导出 |

## 六、领域约束

- **形态**：Web 应用；桌面浏览器优先；云端部署（跨设备 + 分享的前提）
- **技术**：前端 SPA + 后端解析服务；技术选型已授权（唯一硬标准 = 稳定跑通）
- **资源**：能力储备项目，无硬性截止；复用已有云资源（阿里云 / Supabase 生态）
- **数据**：图纸可能含工程敏感信息 → 分享默认只读；后续可加访问控制

## 七、已有资产

- 云资源：阿里云（OSS 等）、Supabase 项目（历史项目在用）
- 本机：Python / Node 开发环境齐全
- 技能库：web-spa、supabase-backend、alicloud-fc-deploy 等可直接调用
- 缺口：真实样例图纸（待补）、DWG 云端解析工具链（待建）

## 八、成功标准（SC）

| # | 条件 → 验证操作 → 预期结果 |
| --- | --- |
| SC1 | 上传含线段 / 多段线 / 文字标注的 DXF → 拆解 → 输出「名称 / X / Y / Z / 关联名称」清单；名称按图纸文字就近关联、缺失自动编号 |
| SC2 | 打开某图纸 3D 视图 → 结构以点 + 连线呈现，可旋转缩放；点选坐标点 → 显示名称与 XYZ |
| SC3 | 完整闭环（上传 → 拆解 → 云端保存 → 列表打开 → 3D → 导出）→ 全链路无报错；刷新 / 换设备数据仍在 |
| SC4 | 修改一条拆解结果（改名 / 删点 / 调关系）→ 保存 → 数据与 3D 同步更新 |
| SC5 | 生成分享链接 → 另一浏览器（未登录）打开 → 只读可见 3D 与数据 |
| SC6 | 上传 .dwg → 后端转换 + 拆解 → 输出与 DXF 路径一致 |

## 九、产出物清单

| 产出物 | 类型 | 接收方 |
| --- | --- | --- |
| CAD 拆解与 3D 还原网页应用 | Web 应用（前端 + 后端） | 用户（及被分享者） |
| 云端部署（可访问 URL） | 部署 | 用户 |
| 使用说明 README | 文档 | 用户 |
| 端到端验证记录 | 文档 | 用户 |

## 十、不做什么

- 不编辑 CAD 原图（只读拆解）——**明确不做**
- 本轮不做（未排除、不承诺）：行业语义层 / 多人协作 / 移动端专项适配
- 本轮不做（未来扩展）：2D 图纸、PDF / 图片形态
- 本轮不做（未来升级）：体量级 / 逼真级 3D

## 十一、待验证假设（Build 前需闭合）

1. DWG → DXF 转换链在云端环境的可行性（工具链选型：LibreDWG / ODA 等，Architect 定案）
2. 名称就近匹配准确率（依赖图纸规范度；需样例实测）
3. 大图纸性能表现（点数规模未知；需样例实测）
