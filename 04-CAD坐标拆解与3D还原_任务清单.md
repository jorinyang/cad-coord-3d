# Build Tasks: cad-coord-3d

Generated from: 01-CAD坐标拆解与3D还原_结构化简报（已批准）
Date: 2026-09-11
状态：待确认

---

## Foundation（基础）

- [ ] **F1 · 转换链打通（本机）**：安装接入 DWG→DXF 转换工具（ODA File Converter 首选 / LibreDWG 备选），建立 converter 抽象接口，对真实 DWG 完成一次转换——输出 DXF 可被 ezdxf 打开且实体数 > 0。_新建。_ 【需样例 DWG；转换失败原文记录】
- [ ] **F2 · 云端基础设施冒烟**：① 最小 Docker 镜像内命令行完成一次 DWG→DXF；② OSS 静态上传冒烟（测试页可访问）；③ FC 部署冒烟（最小服务 HTTP 可访问）——三条通道走通并脚本化记录。_新建。_ 【先盘点 ~/.ClawShell/deploy 既有通道；需阿里云凭据；镜像加速预案】
- [ ] **F3 · 解析器核心**：ezdxf 解析 → 实体提取（LINE / LWPOLYLINE / POLYLINE / POINT）→ 顶点归一（容差去重）→ 连线构建 → 名称匹配（TEXT / MTEXT 就近，阈值初定包围盒对角线 1%）→ 缺名补号（P0001…）→ 输出标准坐标 JSON（03 文档第二节格式）。_新建。_ 【用 ezdxf 自造已知结构测试图做断言：点数 / 线数 / 名称 / 坐标误差 < 1e-6】
- [ ] **F4 · 后端 API**：FastAPI 应用（本地 8788 端口）；POST /api/parse 接收上传 → 按格式分流（DXF 直解 / DWG 转后解）→ 返回坐标 JSON；统一错误信封 + 错误码 + CORS。_新建。_ 【完成标准：curl 上传 DXF 与 DWG 各一次均返回 ok；坏文件返回结构化错误】_依赖：F1、F3。_
- [ ] **F5 · 数据层**：Supabase 建 4 张表（cad_drawings / cad_points / cad_links / cad_share_snapshots）+ 索引 + RLS（anon 只读 / service 写）；前后端连通性验证。_新建。_ 【完成标准：anon 读空表成功、写被拒；service 写成功；复用现有项目 + cad_ 前缀】

## Core（核心构建）

- [ ] **C1 · 前端最小闭环**：前端壳（index.html + ES 模块路由 + 设计令牌 CSS + 基础组件 toast/confirm/loading）+ 上传视图（拖拽 / 选择、进度、错误提示）→ 调 /api/parse → 显示结果概要（N 点 M 线）。_新建。_ 【覆盖 SC1 雏形；本地 http.server 运行】_依赖：F4。_
- [ ] **C2 · 3D 查看器**：Three.js（vendor 本地化）——点 / 线渲染、旋转缩放（OrbitControls）、点选显示名称 + XYZ（raycaster，12px 阈值）、选中高亮、dispose 生命周期。_新建。_ 【完成标准：10k 点 ≥ 30fps；点选准确；反复切换无泄漏】_依赖：C1。_
- [ ] **C3 · 入库 + 图纸库 + 详情**：解析完成后端写库；图纸库列表（读 Supabase）；详情视图（3D + 数据表只读 + 重命名 / 删除）。_新建。_ 【完成标准：上传→自动入库→列表可见→详情正确；删除清理干净】_依赖：C2、F5。_
- [ ] **C4 · 编辑修正**：数据表编辑（改名 / 删点 / 删边）→ 后端 update API → 保存后 3D 与表格同步刷新。_新建。_ 【覆盖 SC4；刷新后持久】_依赖：C3。_
- [ ] **C5 · 导出**：CSV（UTF-8 BOM）/ XLSX / JSON 三格式导出，文件命名按标准规范。_新建。_ 【完成标准：Excel 打开无乱码、内容与界面一致（含「产生链接的坐标名称」列）】_依赖：C3。_
- [ ] **C6 · 分享**：生成快照（后端写 cad_share_snapshots）→ 分享链接路由（#/share/{token}，只读 3D + 表）→ 撤销分享。_新建。_ 【覆盖 SC5（线上部分在 C7/P2 验证）；无痕窗口可看、撤销即失效】_依赖：C3。_
- [ ] **C7 · 云端部署**：前端 → OSS 正式路径；后端 → FC 容器（含转换工具链）；生产配置（CORS / 环境变量 / 缓存）；部署脚本化。_新建。_ 【完成标准：线上全链「上传→拆解→3D→分享」通过；SC3 换设备数据在】_依赖：C6、F2。_

## Polish（润色与验证）

- [ ] **P1 · 全态打磨**：空态 / 加载态 / 错误态全覆盖、交互动效（克制）、响应式基础适配。覆盖：冷启动、大文件、解析失败、空库、分享失效等边界。_新建。_
- [ ] **P2 · 端到端验证**：SC1–SC6 全量 + 质量门 7 项 + 真实样例实测（≥ 1 DWG + ≥ 1 DXF，抽验 ≥ 3 处）。_新建。_ 【记录于 BUILD_LOG】
- [ ] **P3 · 项目说明文档**：README（架构图、部署 / 更新步骤、使用说明、转换引擎切换与阈值调整 FAQ）。_新建。_ 【他人可照文档复现部署】
- [ ] **P4 · Review：运行 answer Phase 7 审查**：对照审查 + 蓝军压力测试 + 评分。_新建。_

## 执行顺序

```
主线：F1 → F3 → F4 → C1 → C2 → C3 → C4 → C5 → C6 → C7 → P1 → P2 → P3 → P4

可并行 / 穿插：
  · F2 在 F1 后进行（云端通道独立冒烟）
  · F5 在 F4 后、C3 前完成（入库前置）
  · F1 ∥ F3 —— 样例未到时 F3 可先启动（自造图不依赖样例）
```

## 前置需求（阻塞项）

| # | 需求 | 用于 | 兜底 |
| --- | --- | --- | --- |
| 1 | 样例图纸（.dwg + .dxf） | F1 / F2 / P2 | 公开样例下载先行；真实样例到位后标定 |
| 2 | 阿里云凭据（OSS / FC / ACR） | F2 / C7 | 先盘点 ~/.ClawShell/deploy 既有通道 |
